{
  "name": "zasper-e2e",
  "private": true,
  "description": "Browser end-to-end tests. Deliberately not part of ui/package.json, so `make init` and every contributor's npm install stay light.",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0"
  }
}
